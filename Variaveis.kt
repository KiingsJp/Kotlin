package com.kings.kotlinlearn;

// Constante global, quando já se tem o valor antes de rodar o código
const val pi = 3.14;

fun main(){

    // Val é uma variável que recebe um valor uma vez e ele não pode ser alterado
    val densidadeAgua = 1;

    // Var é uma variável que pode ser alterada a qualquer momento
    var valorBitcoin = 130.000
    valorBitcoin = 128.000

    // Assim podemos especificar o tipo da variável
    val nome : String = "João";
    val numero : Long = 34996842970;
    var numEndereco : Int = 577;

    // Sempre utilizar o val, e só alterar para var quando perceber que é necessário ao decorrer do código
}
